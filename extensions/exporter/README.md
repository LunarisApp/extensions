# @lunarisapp/exporter

Standalone Lunaris Exporter extension. Select supported documents and folders, arrange PDF order, and customize page layout, typography, spacing, colors, and page numbers. Settings persist with the exporter resource.

The npm entry point exposes its provider-owned semantic export contract. Extensions declare an optional dependency on `lunaris.exporter` and contribute to `exporterRepresentations`.
