# @lunarisapp/exporter

Standalone Lunaris Exporter extension. The npm entry point exposes its provider-owned semantic export contract. Extensions declare an optional dependency on `lunaris.exporter` and contribute to `exporterRepresentations`.
